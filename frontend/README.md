# Frontend (Vite + React)

Minimal black-and-white React frontend for the API server.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Build:

```bash
npm run build
# optionally: npm run copy-dist to copy `dist/` into apiServer/static
```
